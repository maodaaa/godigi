import 'package:flutter/material.dart';

class AppCornerRadius {
  AppCornerRadius._();

  static const Radius none = Radius.zero;

  static const Radius sm = Radius.circular(4);

  static const Radius md = Radius.circular(8);

  static const Radius ml = Radius.circular(12);

  static const Radius lg = Radius.circular(16);

  static const Radius xl = Radius.circular(32);

  static const Radius full = Radius.circular(9999);
}
